This example can be run on all cores simultaneously.

CM0+ uses 2 sub counters of MCWDT0 and toggles LED1,2 when a fault occurs on upper limit crossing

CM7_0 uses 2 sub counters of MCWDT1 and toggles LED1,2 when a fault occurs on upper limit crossing
